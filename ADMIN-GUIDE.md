# 管理后台 · 使用指南

> 这份文档写给「完全不懂技术」的你。跟着做就行，每一步我都写了你会看到什么。

---

## 目录

- [一、后台在哪里？怎么进？](#一后台在哪里怎么进)
- [二、现在就能用：本地模式](#二现在就能用本地模式)
- [三、正式上线：升级成云端后台（推荐）](#三正式上线升级成云端后台推荐)
- [四、五个模块怎么用](#四五个模块怎么用)
- [五、日常更新流程](#五日常更新流程)
- [六、常见问题](#六常见问题)

---

## 一、后台在哪里？怎么进？

后台地址就是你的网站地址后面加 `/admin.html`，比如：

```
https://one-person-company-ten.vercel.app/admin.html
```

网站每页的最底部（页脚右下角）也有一个 **「管理后台」** 的小链接，点一下就能进去。

进去之后会看到一个绿色背景的登录框，输入邮箱和密码就能进。

---

## 二、现在就能用：本地模式

**在你还没做第三章的配置之前**，后台处于「本地模式」：

| 项目 | 说明 |
|---|---|
| 登录账号 | `monna@local` |
| 登录密码 | `monna2026` |
| 数据存在哪 | **你自己这台电脑的浏览器里** |
| 能做什么 | 熟悉后台操作、写文章草稿、调文案 |
| 限制 | ① 换电脑/换浏览器就看不到了<br>② 别人访问你的网站看不到你改的内容<br>③ **访客提交的预约，你收不到** |

> 想改这个默认账号密码？打开 `config.js`，改 `localAccount` 里的 `email` 和 `password` 就行。

**本地模式适合**：先玩玩看，熟悉一下界面。

**但如果你要真的收客户预约，必须看下一章。**

---

## 三、正式上线：升级成云端后台（推荐）

做完这一章，你的后台就变成「真的」了：

- ✅ 用你自己的邮箱密码登录（不再是人人都知道的默认密码）
- ✅ 访客在预约页提交的信息，**实时进后台**
- ✅ 手机上也能打开后台改内容
- ✅ 换电脑、换浏览器，数据都在
- ✅ 完全免费（Supabase 免费额度对个人站完全够用）

**整个过程大约 10 分钟，分 6 步。**

---

### 第 1 步：注册 Supabase

1. 打开 **https://supabase.com**
2. 点右上角 **「Start your project」**
3. 选择 **「Continue with GitHub」**（用你之前注册的 GitHub 账号登录，最省事）
4. 授权后进入控制台

---

### 第 2 步：创建一个项目

1. 点绿色的 **「New project」** 按钮
2. 填写：

| 字段 | 填什么 |
|---|---|
| Organization | 保持默认（就是你自己的名字） |
| Name | `one-person-company` |
| Database Password | **点右边的「Generate a password」自动生成，然后复制保存到记事本**（这个密码以后基本用不到，但别丢） |
| Region | 选 **Southeast Asia (Singapore)**（离中国最近，速度最快） |
| Pricing Plan | 保持 **Free** |

3. 点 **「Create new project」**
4. **等待 1-2 分钟**，页面会显示进度。等它变成正常的项目面板就好了。

---

### 第 3 步：建数据表（复制粘贴一次）

1. 项目面板左侧菜单，找到 **「SQL Editor」**（图标像个 `>_`），点它
2. 点 **「New query」**
3. 打开你电脑上这个文件：

```
D:\数字大脑\04_Output\Workbuddy输出\2026-09-10-09-42-16\my-one-person-company\supabase-schema.sql
```

用记事本打开，**全选（Ctrl+A）→ 复制（Ctrl+C）**

4. 回到 Supabase，粘贴到那个空白编辑框里
5. 点右下角绿色的 **「Run」** 按钮（或按 Ctrl+Enter）
6. 页面下方出现 **「Success. No rows returned」** 就成功了 ✅

> 如果报错，把错误信息截图发我。

---

### 第 4 步：复制两个值到 config.js

1. Supabase 左侧菜单最下面，点 **「Project Settings」**（齿轮图标）
2. 左侧选 **「API」**
3. 你会看到两个东西，分别复制：

| Supabase 上叫什么 | 复制它 |
|---|---|
| **Project URL** | 形如 `https://abcdefghijk.supabase.co` |
| **Project API keys** 里的 **`anon` `public`** | 一长串 `eyJhbGci...` 开头的字符 |

> ⚠️ 复制的是 **anon public** 那一行，**不是** `service_role`。service_role 是最高权限密钥，绝对不能放到网站里。

4. 回到你电脑，用记事本打开 `config.js`，把两个值填进去：

```js
window.WB_CONFIG = {

  /* ① Supabase 项目地址 */
  supabaseUrl: 'https://abcdefghijk.supabase.co',        ← 换成你的

  /* ② Supabase anon public 密钥 */
  supabaseAnonKey: 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9....',   ← 换成你的

  ...
};
```

5. **保存文件**（Ctrl+S）

> 💡 这两个值放在网页代码里是**安全的**——Supabase 用「行级权限」控制：访客只能看内容、只能提交预约，看不到你的线索。

---

### 第 5 步：创建你自己的登录账号

1. Supabase 左侧菜单点 **「Authentication」**
2. 点 **「Users」**
3. 点右上角 **「Add user」** → **「Create new user」**
4. 填：

| 字段 | 填什么 |
|---|---|
| Email | 你常用的邮箱（这就是以后登录后台的账号） |
| Password | 设一个**强密码**，记在密码管理器里 |
| **Auto Confirm User** | ✅ **一定要勾上** |

5. 点 **「Create user」**

---

### 第 6 步：把改好的文件传上去

现在 `config.js` 变了，需要重新上传到 GitHub，Vercel 才会生效。

1. 打开你的 GitHub 仓库：**https://github.com/gehmwendy92-a11y/one-person-company**
2. 点 **「Add file」** → **「Upload files」**
3. 把下面这些**新文件**拖进去（Ctrl+A 全选整个文件夹里的文件也行，同名的会自动覆盖）：

```
config.js          ← 必传（刚改过的）
seed.js            ← 必传（新文件）
store.js           ← 必传（新文件）
site.js            ← 必传（新文件）
admin.html         ← 必传（新文件）
admin.js           ← 必传（新文件）
admin.css          ← 必传（新文件）
supabase-schema.sql ← 可选（留着当备份）
index.html / services.html / about.html /
articles.html / article.html / booking.html /
main.js            ← 必传（都改过了）
```

4. 滚到底部，备注写：`添加管理后台`，点 **「Commit changes」**
5. 等 1-2 分钟，Vercel 会自动重新部署

---

### ✅ 验证

1. 打开 `https://你的域名/admin.html`
2. 这次登录框下面应该显示 **「云端模式」**
3. 用**第 5 步创建的邮箱密码**登录
4. 进后台后，去「产品服务管理」随便改个字，保存
5. 打开网站首页，看看是不是变了

**如果都正常，恭喜你——后台正式上线了 🎉**

---

## 四、五个模块怎么用

### 1. 首页档案管理

管的是**访客第一眼看到的内容**：

- 首屏小标签、主标题、副标题
- 两个按钮的文字
- 中间那排大数字（读者数、工作年限等）
- 页面最下方的绿色行动区
- **联系方式**（微信号、联系邮箱）——填写后会显示在「关于我」和「预约对话」页面的"联系我"模块，邮箱会变成可点击的邮件链接；**两项都留空则该模块不显示**

> 改完点「保存首页内容」，刷新网站首页就能看到。

---

### 2. 产品服务管理

管的是**你卖什么、多少钱**：

- 服务名称、一句话副标题、详细介绍
- 价格（比如 `¥299 / 45 分钟`）
- 服务包含的内容（可以一条条加）
- 图形区大字（比如 `1:1`、`3h`）
- 是否在网站上显示

> 可以随时「新增服务」或「删除服务」。删掉的服务在网站上也会消失。

---

### 3. 关于我的介绍管理

管的是**「关于我」那一页**：

- 页面大标题和一句话介绍
- 左侧卡片上的名字和坐标
- **我的故事**（支持 Markdown：空行分段、`**加粗**`）
- 关键节点（时间线）
- 我相信的几件事（价值观）
- 我的工具箱

> 图标那栏：价值观可以填 `question` / `check` / `heart` / `chart` / `globe` / `people`；工具箱可以直接填 emoji，比如 `📐`。

---

### 4. 我的文章管理

管的是**你写的东西**：

- **写新文章**：标题、日期、摘要、标签、封面大字、封面背景图、正文
- **上传 Markdown 文件**：在「正文」上方点「📄 上传 Markdown 文件」，选择 `.md` 文件后，正文会自动填充；如果标题为空，还会自动提取文件里的 `# 一级标题`
- **封面背景图**：在「封面大字」下方上传图片；留空则使用默认的绿色渐变背景；点「恢复默认」可清空已上传图片
- 正文用 **Markdown** 写，支持：
  - `# 一级标题` `## 二级标题`
  - `**加粗**` `*斜体*`
  - `- 列表项`
  - `> 引用`
  - `[链接文字](网址)`
  - 表格、代码块
- 「预览」按钮可以先看看效果
- 取消勾选「发布」，文章就变成草稿，网站上不显示

> 保存后，网站「我的文章」页和首页的「最近在写的东西」会自动更新。

---

### 5. 预约咨询线索管理

这是**最值钱的一页**——访客在预约页填的信息都会进这里。

| 功能 | 说明 |
|---|---|
| 看到什么 | 提交时间、姓名、联系方式、想约什么服务、期望时间、城市、想聊什么、来源渠道 |
| 改状态 | 每条右边有个下拉框：新提交 → 已联系 → 已预约 → 已完成 / 已关闭 |
| 导出 | 右上角「导出 CSV」，用 Excel 打开，可以做自己的客户表 |
| 删除 | 垃圾线索直接删掉 |

> 左侧菜单「预约咨询」旁边的红色数字 = **还没处理的新线索数量**。

---

### 6. 访问数据统计（埋点）

网站自带轻量访问统计，**只记行为、不记任何个人信息**，数据存在你自己的数据库里。

**⚠️ 云端模式需要先建一次表**：Supabase → SQL Editor → 粘贴运行项目文件夹里的 `supabase-events.sql`（就一次，以后不用再管）。

**自动记录的行为**：

| 行为 | 什么时候记 |
|---|---|
| 页面访问 | 访客每打开一个页面 |
| 文章阅读 | 访客点开某篇文章 |
| 点击预约按钮 | 访客点「立即预约」等按钮 |
| 提交预约 | 访客成功提交预约表单 |
| 点击联系方式 | 访客点微信号或邮箱 |

**页面上能看到**：
- 今日访问 / 累计访问 / 今日新线索 / 累计线索 四个数字卡
- 哪些页面被看过（条形图）
- 访客做了什么（关键行为统计）
- 最受欢迎的文章 TOP5
- 访客从哪来（来源域名）
- 最近 30 条明细记录

**想埋新的点？** 前台任何位置加一句 `WBTrack('事件名', '标签')` 即可，保存后自动进这个模块。

---

## 五、日常更新流程

**以后你改内容，只需要在后台点点点，不用再碰 GitHub。**

```
改内容 → 点保存 → 刷新网站，立刻生效
```

只有两种情况需要重新上传文件：

1. 改了 `config.js`（比如换 Supabase 项目）
2. 改了网站的设计/结构

---

## 六、常见问题

### Q1：登录提示「邮箱或密码不对」

- 确认用的是**第 5 步在 Supabase 里创建的那个账号**，不是 `monna@local`
- 检查密码大小写
- 如果提示「邮箱还没验证」，去 Supabase → Authentication → Users，找到你的账号，点右边的 `···` → **Confirm email**

---

### Q2：后台能登录，但看不到数据 / 保存报错

多半是 `config.js` 里的两个值填错了。检查：

- `supabaseUrl` 结尾**没有**多余的斜杠（应该是 `https://xxx.supabase.co`，不是 `https://xxx.supabase.co/`）
- `supabaseAnonKey` 复制完整了（很长一串，别漏掉尾巴）
- 改完 `config.js` 后，**有没有重新上传到 GitHub 并等 Vercel 部署完**

---

### Q3：网站上没看到我改的内容

1. 先按 **Ctrl + Shift + R** 强制刷新（浏览器有缓存）
2. 确认保存时提示「已保存」
3. 打开 `你的域名/admin.html`，看看后台里的数据是不是真的改了
4. 如果后台改了但网站没变，等 1 分钟再刷（Vercel 部署有延迟）

---

### Q4：访客提交的预约收不到

- 确认已经完成第三章（云端模式）
- 打开 `你的域名/admin.html`，看「预约咨询线索管理」里有没有
- 如果访客说提交了但后台没有：去 Supabase → **Table Editor** → `leads` 表看看有没有数据
  - 表里有数据 → 是后台读取的问题，把浏览器控制台（F12）的报错发我
  - 表里也没数据 → 是访客提交的问题，同样发我报错

---

### Q5：Supabase 会不会收费？

不会。免费版给 500MB 数据库 + 5 万月活用户，个人网站**远远用不完**。

唯一要注意：**免费项目如果连续 7 天完全没人访问，会被自动暂停**。不过你的网站只要有人打开就会触发，一般不会遇到。真被暂停了，登录 Supabase 点一下「Restore」就好。

---

### Q6：我想改后台的登录密码

Supabase → **Authentication** → **Users** → 找到你的账号 → 右边 `···` → **Reset password**（或直接删掉重新建一个）。

---

### Q7：本地模式的数据能搬到云端吗？

目前不行，需要手动复制。建议**直接用云端模式**，本地模式只用来熟悉界面。

---

## 附：文件说明

| 文件 | 作用 | 你需要动它吗 |
|---|---|---|
| `admin.html` | 后台页面 | 不用 |
| `admin.css` | 后台样式 | 不用 |
| `admin.js` | 后台逻辑 | 不用 |
| `config.js` | **配置（Supabase 地址和密钥）** | **要改一次** |
| `store.js` | 数据读写 | 不用 |
| `seed.js` | 初始内容 | 不用 |
| `site.js` | 网站读取后台内容 | 不用 |
| `supabase-schema.sql` | 建表脚本 | 复制粘贴一次 |
| `style.css` | 网站样式 | 想改配色时改 |
| `main.js` | 网站交互 | 不用 |

---

有任何一步卡住，把**你看到的画面截图**发我，我帮你看。
